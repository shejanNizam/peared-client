import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pearedClient',
  appName: 'peared-client',
  webDir: 'public'
};

export default config;
